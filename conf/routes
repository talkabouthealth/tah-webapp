# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Modules
*		/										module:secure

GET     /                                       Application.index
GET     /signup/?                               Application.signup
POST    /register/?	                            Application.register
GET     /forgotpassword/?                       Application.forgotPassword
POST    /sendnewpassword/?						Application.sendNewPassword
GET     /contactus/?							Application.contactus
POST    /contactus/?							Application.sendContactEmail

GET		/verify/{verifyCode}					Profile.verifyEmail

GET		/home/?									Home.index
GET		/dashboard/?							Dashboard.index

GET		/talk/{topicId}							Talk.talkApp
GET		/talk/?									Talk.talkApp

GET		/topic/{tid}							Topics.viewTopic

GET		/image/{userName}						Image.show

# Map static resources from the /app/public folder to the /public path
GET     /public/                                staticDir:public

GET		/{userName}								PublicProfile.view
GET		/{userName}/thankyous					PublicProfile.thankyous

# Catch all
*       /{controller}/{action}                  {controller}.{action}

					
